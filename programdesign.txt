Planning the functions, objects and methods required for this app.
Should we use object literals or Constructors?

////////// Objects: /////////////////////////////////
userVoice {
  micOn:Bool,
  hasInput:Bool,
  frequency:Number,
  amplitude:Number,
  turnMicOn : function() {},
  turnMicOff : function() {}
}

userHead {
  camOn:Bool,
  hasHead:Bool,
  xPos:Number,
  yPos:Number,
  turnCamOn : function() {},
  turnCamOff : function() {}
}


///////////// Functions: //////////////////////////////
function drawEmoji (emojiUnicode:String, size:Number, x:Number, y:Number) {
  //draws given emoji at given size and location
}

function getPitch () {
  //gets current frequency and amplitude from audio input
  //then updates userVoice freq/amp params
  //must be running fairly continuously in order to update
}
